# Some of the commands might be needed in the near future

1. To run project

```bash
npm run dev
```

2. To run the fake server created with the help of json-server package and with json file present in backend folder

```bash
npx json-server --watch backend/db.json --port=3000
```
